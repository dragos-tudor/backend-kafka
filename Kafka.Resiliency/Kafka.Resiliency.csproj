<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Wraps Kafka.StateMachines's consume, resume, and relay operations in supervised loops with backoff and restart, keeping kafka messages processing alive across transient failures.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Kafka.StateMachines/Kafka.StateMachines.csproj" />
  </ItemGroup>

  <ItemGroup Condition="'$(PackOnly)' != 'true'">
    <PackageReference Include="DistributedLock.FileSystem" />
  </ItemGroup>

</Project>
