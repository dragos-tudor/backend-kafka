<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Wraps consume, resume, and relay pipelines in supervised loops with backoff and restart, keeping kafka messages processing alive across transient failures.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Kafka.Pipelines/Kafka.Pipelines.csproj" />
  </ItemGroup>

  <ItemGroup Condition="'$(PackOnly)' != 'true'">
    <PackageReference Include="DistributedLock.FileSystem" />
  </ItemGroup>

</Project>
